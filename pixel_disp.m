function y = pixel_disp()
    
    %choose a pixel
    %for column start of search window till this end value
        %for row start of search window till this end value
            %use support_cmp sending:
                %TWO IMAGES
                %window size
                %location for one pixel
                %location for second pixel (col and row from loooooooooop)
            %save the values if the minimum difference is received
    y = 0